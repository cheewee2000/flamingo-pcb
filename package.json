{
  "name": "flamingo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test -ws --if-present",
    "build": "npm run build -ws --if-present",
    "serve": "node packages/server/dist/cli.js serve"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^3"
  }
}
